# FinCrime Dojo — Dependencies
# Install with: pip install -r requirements.txt

# Core App Framework
streamlit>=1.35.0

# AI Brain
google-generativeai>=0.7.0

# Data Manipulation
pandas>=2.0.0
numpy>=1.26.0

# Database (built into Python stdlib — no install needed)
# sqlite3  ← already included

# Machine Learning (for Architect-level lessons)
scikit-learn>=1.4.0
xgboost>=2.0.0
imbalanced-learn>=0.12.0

# Visualization (optional but rendered in Streamlit)
matplotlib>=3.8.0
seaborn>=0.13.0

# Fuzzy Matching (for Investigator-level PEP screening)
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0

# Network Analysis (for shell company detection)
networkx>=3.2.0

# Utilities
python-dateutil>=2.9.0
